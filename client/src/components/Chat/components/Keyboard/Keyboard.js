import React from "react"
import "./Keyboard.scss"

const keyboard = () => {
    return (
        <div className="keyboard-wrapper">
            keyboard
        </div>
    )
};

export default keyboard
